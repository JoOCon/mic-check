import React from 'react';
import ReactDOM from 'react-dom';
import App from '../';

describe('App Tests', () => {
  it('should render App with the correct props', () => {
    expect(true).toEqual(true)
  });
})
